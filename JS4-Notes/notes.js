// template literal- a method of printing verbatim into the console, or returning input directly back as it is recieved. 
// by putting code in `  `, it will return whatever is in the `s, to add operations, use ${} within the backticks.
 